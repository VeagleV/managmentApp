/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./index.html", "./src/**/*.{vue,js,ts,jsx,tsx}"], // Укажите пути к вашим файлам
  theme: {
    extend: {}, // Здесь можно расширить тему (например, цвета, шрифты)
  },
  plugins: [], // Добавьте плагины, если они нужны
};
